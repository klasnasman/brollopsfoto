---
export interface Props {
  first: string;
  second: string;
}

const { first, second } = Astro.props;
---

<div class="modal">
  <p>{first}</p>
  <p>{second}</p>
</div>

<style>
  .modal {
    display: flex;
    position: fixed;
    justify-content: space-between;
    width: var(--container-width);
    left: calc((100% - var(--container-width)) / 2);
    top: calc((50% - var(--header-height)));
    font-size: var(--step-1);
    transition:
      opacity var(--ease-0) ease-out,
      filter var(--ease-0) ease-out,
      z-index var(--ease-0) ease-out;
    opacity: 1;
    filter: blur(0px);
    z-index: 1;
  }

  .modal.close {
    opacity: 0;
    filter: blur(10px);
    z-index: -1;
  }
</style>
